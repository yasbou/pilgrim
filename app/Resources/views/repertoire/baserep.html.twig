<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Le Repertoire{% endblock %}</title>
        <link rel="stylesheet" href="{{asset('css/reset.css')}}">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
        <link rel="stylesheet" href="{{asset('css/stylehomerep.css')}}">
        <link rel="stylesheet" href="{{asset('css/stylelistAgence.css')}}">
        <link rel="stylesheet" href="{{asset('css/stylelistHotels.css')}}">



        <link href="https://fonts.googleapis.com/css?family=Mukta+Mahee:300" rel="stylesheet">
    </head>
    <body>
        {% block body %}

        <header>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">

          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>

          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
              <li class="nav-item active">
                <a class="nav-link" href="{{path('homepage')}}">PILGRIM <span class="sr-only">(current)</span>.com</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="{{path('home_repertoire')}}">Accueil</a>
              </li>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  Region
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                  {% for R in region %}

        				<a class="dropdown-item" href="{{path('list_by_region', {'id':R.id, 'page': 1})}}">{{ R.nom }}</a>
        				{% endfor %}

                </div>
              </li>
                <li class="nav-item dropdown">
                  <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Ville
                  </a>
                  <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    {% for V in ville %}
          				<a class="dropdown-item" href="{{path('list_by_ville', {'id':V.id, 'page': 1})}}">{{ V.nomVille }}</a>
          				{% endfor %}

                  </div>
              </li>
              <li class="nav-item">
                <a class="nav-link disabled" href="#">Contact</a>
              </li>
            </ul>
          </div>
        </nav>

        </header>


        {% endblock %}



        {% block javascripts %}

        <script defer src="https://use.fontawesome.com/releases/v5.0.6/js/all.js"></script>

        <script language="javascript">
        // On crée une variable JC dans le template
        // dans une balise script afin que les scripts JS suivants
        // aient accès à cette variable
        ajaxCheckNumber = '{{ path('hotel_commentaire') }}';
        </script>
        <script
			     src="http://code.jquery.com/jquery-3.3.1.min.js"
			     integrity="sha256-FgpCb/KJQlLNfOu91ta32o/NMZxltwRo8QtmkMRdAu8="
		       crossorigin="anonymous">
        </script>

        <script src= "{{asset("js/commentaire_hotel.js")}}" type="text/javascript"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js" integrity="sha384-JZR6Spejh4U02d8jOt6vLEHfe/JQGiRRSQQxSfFWpi1MquVdAyjUar5+76PVCmYl" crossorigin="anonymous"></script>

        {% endblock %}
    </body>
</html>
