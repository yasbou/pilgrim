{% extends 'admin/baseadmin.html.twig' %}

{% block body %}
{{ parent() }}
{% include 'admin/header.html.twig' %}
<div class="bodyadmin">
    <h1>Commentairehotel</h1>

    <table class="table table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaireHotel.id }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ commentaireHotel.auteur }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ commentaireHotel.date }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ commentaireHotel.commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a class="btn btn-primary btn-sm"  href="{{ path('admin_repertoire_commentairehotel_index') }}">Back to the list</a>
        </li>
        <li>
            <a class="btn btn-warning btn-sm" href="{{ path('admin_repertoire_commentairehotel_edit', { 'id': commentaireHotel.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer" class="btn btn-danger btn-sm" >
            {{ form_end(delete_form) }}
        </li>
    </ul>
    </div>
{% endblock %}
